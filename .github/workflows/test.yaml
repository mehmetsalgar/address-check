name: Test - Pull Request
on:
  pull_request:
    types:
      - opened
    branches:
      - 'feature/**'
jobs:
  print-start:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "start"